apiVersion: v1
kind: Service
metadata:
  name: tars-docs
  namespace: tars
  labels:
    app: tars-docs
spec:
  selector:
    app: tars-docs
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
