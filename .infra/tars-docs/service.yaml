apiVersion: v1
kind: Service
metadata:
  name: tars-docs
  namespace: tars
  labels:
    app: tars-docs
spec:
  selector:
    app: tars-docs
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5252
      targetPort: 5252
      name: http
